[project]
name = "single-tree-detection"
version = "0.1.0"
description = "Neural network for single tree object detection using satellite and LIDAR data"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.5",
]
